<div class="wrapper-team">
  <div class="container">
    <h1 class="text-center">Our Team</h1>
    <ul class="list-inline text-center">
      <% ["joe", "andy"].each do |member| %>
        <li>
          <h3><%= member.capitalize %></h3>
          <%= image_tag "#{member}.jpg", class: "img-circle avatar-large" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>

